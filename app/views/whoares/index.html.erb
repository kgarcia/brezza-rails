<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
      <h2 class="text-dark-blue"><strong>Quienes Somos</strong></h2>
      <ol class="breadcrumb">
        <li>
          <a href="<%= url_for :controller => 'home', :action => 'index' %>">Inicio</a>
        </li>
        <li class="active">
            <strong class="text-blue">Listado</strong>
        </li>
    </ol>
  </div>
</div>
<div class="wrapper wrapper-content">
    <div class="row">
      <div class="col-sm-12">
        <div class="ibox float-e-margins">
          <div class="ibox-title">
              <h4 class="text-dark-blue">
                <strong>Campos: </strong>
              </h4>
          </div>
          <div class="ibox-content">
            <%= link_to "Registrar Campos", new_whoare_path, :class => "btn btn-primary btn-sm", style: 'position: absolute; z-index: 9999; margin-top: -30px;'%>

  <table class="footable table table-stripped" data-sorting="true" data-filtering="true">
  <thead>
    <tr>
      <th>Descrición</th>
      <th>Brezza Project</th>
      <th>Misión</th>
      <th>Visión</th>
      <th>Reseña historica</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @whoares.each do |whoare| %>
      <tr>
        <td><%= whoare.description %></td>
        <td><%= whoare.brezzastory %></td>
        <td><%= whoare.mission %></td>
        <td><%= whoare.vision %></td>
        <td><%= whoare.history %></td>
        <td><%= link_to 'Ver', whoare, :class => "btn btn-primary btn-sm" %></td>
        <td><%= link_to 'Editar', edit_whoare_path(whoare) , :class => "btn btn-warning btn-sm" %></td>
        <td><%= link_to 'Eliminar', whoare, method: :delete, data: { confirm: '¿Está seguro que desea eliminar las descripciones?' } %></td>

      </tr>
    <% end %>
  </tbody>
 </table>
          </div>
        </div>
      </div> 
  </div>
</div>





<p id="notice"><%= notice %></p>

<h1>Whoares</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Brezzastory</th>
      <th>Mission</th>
      <th>Vision</th>
      <th>History</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @whoares.each do |whoare| %>
      <tr>
        <td><%= whoare.description %></td>
        <td><%= whoare.brezzastory %></td>
        <td><%= whoare.mission %></td>
        <td><%= whoare.vision %></td>
        <td><%= whoare.history %></td>
        <td><%= link_to 'Show', whoare %></td>
        <td><%= link_to 'Edit', edit_whoare_path(whoare) %></td>
        <td><%= link_to 'Destroy', whoare, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Whoare', new_whoare_path %>
